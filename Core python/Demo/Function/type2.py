# with passing parameters(input)
# withiout returing output

def addition(num1, num2): # formal parameter / arguments
    sum = num1+num2
    print(f"The sum two numbers is{sum}")

n1 = int(input("Enter the number1:"))
n2 = int(input("Enter the numbber2:"))

addition(n1,n2) # Actual parameter / arguments